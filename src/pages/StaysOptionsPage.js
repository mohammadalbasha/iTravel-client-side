import StaysOptions from "../components/Stays/StaysOptions";

const StaysOptionsPage = () => {
  document.title = 'iTravel | Stays Options';
  return <StaysOptions />;
};

export default StaysOptionsPage;